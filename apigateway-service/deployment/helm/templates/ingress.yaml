{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "apigateway-app.fullname" . }}-ingress
  labels:
    {{- include "apigateway-app.labels" . | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "apigateway-app.fullname" . }}
                port:
                  number: {{ .Values.servicePort }}
{{- end }}
