apiVersion: v1
kind: Service
metadata:
  name: {{ include "apigateway-app.fullname" . }}
  labels:
    {{- include "apigateway-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "apigateway-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
