apiVersion: v1
kind: Service
metadata:
  name: {{ include "transaction-app.fullname" . }}
  labels:
    {{- include "transaction-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "transaction-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
