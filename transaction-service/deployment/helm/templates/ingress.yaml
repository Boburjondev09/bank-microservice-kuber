{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "transaction-app.fullname" . }}-ingress
  labels:
    {{- include "transaction-app.labels" . | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "transaction-app.fullname" . }}
                port:
                  number: {{ .Values.servicePort }}
{{- end }}
