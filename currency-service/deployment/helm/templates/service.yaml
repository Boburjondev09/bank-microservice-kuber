apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmway-app.fullname" . }}
  labels:
    {{- include "helmway-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helmway-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http