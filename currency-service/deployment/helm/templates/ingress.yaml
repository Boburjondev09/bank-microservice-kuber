{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "helmway-app.fullname" . }}-ingress
  labels:
    {{- include "helmway-app.labels" . | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "helmway-app.fullname" . }}
                port:
                  number: {{ .Values.servicePort }}
  {{- end }}