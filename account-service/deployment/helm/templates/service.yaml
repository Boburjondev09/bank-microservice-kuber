apiVersion: v1
kind: Service
metadata:
  name: {{ include "account-app.fullname" . }}
  labels:
    {{- include "account-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "account-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
