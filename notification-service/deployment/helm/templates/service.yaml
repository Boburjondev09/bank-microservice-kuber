apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification-app.fullname" . }}
  labels:
    {{- include "notification-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "notification-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
